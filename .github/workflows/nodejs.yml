name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x]

    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: npm install
      run: cd proof.runner && npm install --no-package-lock
    - name: npm ls
      run: npm ls
    - name: npm install nyc, mocha, codecov
      run:  npm install -g nyc mocha codecov
    - name: npm test
      run: cd proof.runner && nyc npm test
      env:
        CI: true
    - name: generate codecov report
      run: cd proof.runner && nyc report --reporter=text-lcov > coverage.lcov
    - name: ship coverage to codecov
      run: cd proof.runner && codecov --branch=${GITHUB_REF##*/}
      env:
        CODECOV_TOKEN: ${{secrets.CODECOV_TOKEN}}
